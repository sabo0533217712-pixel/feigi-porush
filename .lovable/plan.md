## שינוי סימוני חסימת זמן/הפסקות

ב-`src/pages/admin/AdminCalendar.tsx`:

1. **תצוגת חודש (שורה 1305)** – הנקודה של חסימת הזמן תהפוך לעיגול אפור מלא במקום מתאר אפור מקווקו:
   - מ-`border border-dashed border-muted-foreground`
   - ל-`bg-muted-foreground` (זהה בגודל לשאר הנקודות, רק בצבע אפור).

2. **תצוגת יום – בלוקים של הפסקות וחסימות זמן (שורות 985–1040 ו-2600–2625)** – להחליף את `repeating-linear-gradient` (הקווים המלוכסנים) במילוי אפור אחיד בשקיפות נמוכה:
   - `background: hsl(var(--muted-foreground) / 0.18)` (או דומה), בלי הקווים.
   - הגבול/הטקסט שכבר קיימים נשארים כפי שהם.

אין שינוי בלוגיקה, ב-DB או בהרשאות.